---
- name: Restart SSH
  become: true
  ansible.builtin.service:
    name: ssh
    state: restarted
  tags:
    - ssh_signed_hostkeys

- name: Restart SSH
  become: true
  ansible.builtin.service:
    name: ssh
    state: restarted
  tags:
    - ssh_signed_hostkeys
